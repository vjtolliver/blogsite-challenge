{{#each posts as |post| }}
<div class="row align-center mb-5">
  <div class="col-md-6 users-post">
    <h2><a href="/dashboard/updatepost/{{post.id}}">{{post.title}}</a></h2>
  </div>
</div>
{{/each}}